#include<stdio.h>
#include<ctype.h>
int main(){
    int a;
    scanf("%d",&a);
    if(isupper(a)){
        printf("Uppercase");
    }
    else(islower(b)){
        printf("Lowercase");
    }
    return 0;

}